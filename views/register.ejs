<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Register</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
</head>
<body class="bg-gradient-to-br from-gray-900 via-black to-gray-800 min-h-screen flex items-center justify-center px-4 text-white">

  <div id="registerBox" class="w-full max-w-md p-8 rounded-2xl bg-gray-900 shadow-2xl border border-gray-800">
    <h1 class="text-3xl font-extrabold mb-6 text-center text-blue-400 tracking-wide">📝 Create Account</h1>

    <form action="/register" method="POST" class="space-y-6">
      <div>
        <label for="userName" class="block text-sm font-medium text-gray-300 mb-1">Username</label>
        <input
          type="text"
          name="userName"
          id="userName"
          placeholder="Enter your name"
          required
          class="w-full px-4 py-3 rounded-lg bg-gray-800 text-white placeholder-gray-500 border border-gray-700 focus:ring-2 focus:ring-blue-500 focus:outline-none"
        />
      </div>

      <div>
        <label for="email" class="block text-sm font-medium text-gray-300 mb-1">Email</label>
        <input
          type="email"
          name="email"
          id="email"
          placeholder="Enter your email"
          required
          class="w-full px-4 py-3 rounded-lg bg-gray-800 text-white placeholder-gray-500 border border-gray-700 focus:ring-2 focus:ring-blue-500 focus:outline-none"
        />
      </div>

      <div>
        <label for="password" class="block text-sm font-medium text-gray-300 mb-1">Password</label>
        <input
          type="password"
          name="password"
          id="password"
          placeholder="Enter your password"
          required
          class="w-full px-4 py-3 rounded-lg bg-gray-800 text-white placeholder-gray-500 border border-gray-700 focus:ring-2 focus:ring-blue-500 focus:outline-none"
        />
      </div>

      <div>
        <span class="block text-sm font-medium text-gray-300 mb-2">Admin Role:</span>
        <div class="flex items-center space-x-4">
          <label class="flex items-center space-x-2">
            <input type="radio" name="isAdmin" value="true" class="form-radio text-blue-500">
            <span>Yes</span>
          </label>
          <label class="flex items-center space-x-2">
            <input type="radio" name="isAdmin" value="false" checked class="form-radio text-blue-500">
            <span>No</span>
          </label>
        </div>
      </div>

      <button
        type="submit"
        class="w-full py-3 rounded-lg bg-blue-500 hover:bg-blue-600 font-semibold text-white text-lg shadow-lg hover:shadow-blue-700 focus:outline-none"
      >
        Register
      </button>
    </form>
  </div>

  <script>
    gsap.from("#registerBox", {
      opacity: 0,
      y: 50,
      duration: 1,
      ease: "power2.out"
    });

    gsap.from("input, button", {
      opacity: 0,
      y: 20,
      duration: 0.8,
      delay: 0.3,
      stagger: 0.2,
      ease: "power2.out"
    });
  </script>

</body>
</html>
